# Руководство по началу разработки - Memento Mori

**Дата:** 2025-12-07
**Версия:** 1.0
**Статус:** Активное руководство по разработке

---

## 📋 Содержание

1. [Предварительные требования](#предварительные-требования)
2. [Первоначальная настройка](#первоначальная-настройка)
3. [Среда разработки](#среда-разработки)
4. [Структура проекта](#структура-проекта)
5. [Запуск приложения](#запуск-приложения)
6. [Рабочий процесс разработки](#рабочий-процесс-разработки)
7. [Общие задачи](#общие-задачи)
8. [Устранение неполадок](#устранение-неполадок)
9. [Следующие шаги](#следующие-шаги)

---

## Предварительные требования

### Необходимое ПО

| ПО | Версия | Назначение | Установка |
|----|--------|------------|-----------|
| **Node.js** | 20+ | Среда выполнения | [nodejs.org](https://nodejs.org/) |
| **npm** | 10+ | Менеджер пакетов | Поставляется с Node.js |
| **Docker** | 20+ | Контейнеризация | [docker.com](https://www.docker.com/) |
| **Docker Compose** | 2.0+ | Оркестрация контейнеров | Поставляется с Docker |
| **Git** | 2.30+ | Контроль версий | [git-scm.com](https://git-scm.com/) |
| **VS Code** | Latest | Рекомендуемая IDE | [code.visualstudio.com](https://code.visualstudio.com/) |

### Системные требования

- **ОС:** macOS, Linux или Windows (рекомендуется WSL2)
- **ОЗУ:** Минимум 8GB, рекомендуется 16GB
- **Дисковое пространство:** 10GB свободного места
- **CPU:** Рекомендуется многоядерный процессор

---

## Первоначальная настройка

### 1. Клонировать репозиторий

```bash
# Клонировать репозиторий
git clone https://github.com/your-org/memento-mori.git
cd memento-mori

# Переключиться на ветку разработки
git checkout develop
```

### 2. Установить зависимости

```bash
# Установить корневые зависимости
npm install

# Установить зависимости для всех приложений
npm run install:all
```

### 3. Конфигурация окружения

```bash
# Скопировать шаблон окружения
cp .env.example .env

# Отредактировать .env файл с вашей конфигурацией
# См. docs/DEVELOPMENT.md для всех переменных окружения
```

**Необходимые переменные окружения:**

```env
# База данных
DATABASE_URL=postgresql://user:password@localhost:5432/mementomori

# Keycloak
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=memento-mori
KEYCLOAK_CLIENT_ID=memento-mori-backend
KEYCLOAK_CLIENT_SECRET=your-secret

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Email
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=your-email
SMTP_PASSWORD=your-password
```

### 4. Запустить Docker сервисы

```bash
# Запустить все Docker сервисы (PostgreSQL, Redis, Keycloak, MailHog)
docker-compose -f docker-compose.dev.yml up -d

# Проверить, что сервисы запущены
docker-compose -f docker-compose.dev.yml ps
```

### 5. Настройка базы данных

```bash
# Запустить миграции базы данных
npm run db:migrate

# Заполнить базу данных тестовыми данными
npm run db:seed

# (Опционально) Открыть Prisma Studio для просмотра базы данных
npm run db:studio
```

### 6. Настройка Keycloak

```bash
# Запустить скрипт настройки Keycloak
./scripts/setup-keycloak.sh

# Или настроить Keycloak вручную
# См. docs/KEYCLOAK_SETUP.md для деталей
```

---

## Среда разработки

### Настройка VS Code

#### Рекомендуемые расширения

Установите эти расширения VS Code:

```json
{
  "recommendations": [
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "prisma.prisma",
    "bradlc.vscode-tailwindcss",
    "ms-vscode.vscode-typescript-next",
    "eamodio.gitlens",
    "ms-vscode.vscode-json"
  ]
}
```

### Скрипты разработки

#### Скрипты корневого уровня

```bash
# Разработка
npm run dev              # Запустить все сервисы в режиме разработки
npm run dev:server       # Запустить только backend
npm run dev:client       # Запустить только client app
npm run dev:vendor       # Запустить только vendor portal
npm run dev:admin        # Запустить только admin portal

# Сборка
npm run build            # Собрать все сервисы
npm run build:server     # Собрать только backend

# Тестирование
npm run test             # Запустить все тесты
npm run test:watch       # Запустить тесты в режиме watch
npm run test:e2e         # Запустить E2E тесты
npm run test:cov         # Сгенерировать отчет о покрытии

# База данных
npm run db:migrate       # Запустить миграции
npm run db:seed          # Заполнить базу данных
npm run db:studio        # Открыть Prisma Studio
npm run db:reset         # Сбросить базу данных (⚠️ деструктивно)

# Docker
npm run docker:up        # Запустить Docker сервисы
npm run docker:down       # Остановить Docker сервисы
npm run docker:logs      # Просмотреть логи Docker

# Линтинг и форматирование
npm run lint             # Запустить ESLint
npm run lint:fix         # Исправить ошибки ESLint
npm run format           # Запустить Prettier
```

---

## Структура проекта

### Структура директорий

```
MM/
├── apps/
│   ├── server/              # Backend API (NestJS)
│   │   ├── src/
│   │   │   ├── auth/        # Модуль аутентификации
│   │   │   ├── vendors/     # Управление поставщиками
│   │   │   ├── services/    # Каталог услуг
│   │   │   ├── orders/      # Обработка заказов
│   │   │   ├── payments/    # Обработка платежей
│   │   │   └── ...
│   │   ├── prisma/          # Схема БД и миграции
│   │   └── package.json
│   │
│   ├── client/              # Client App (Next.js)
│   │   ├── src/
│   │   │   ├── app/         # Next.js app router
│   │   │   ├── components/  # React компоненты
│   │   │   └── lib/         # Утилиты и API клиенты
│   │   └── package.json
│   │
│   ├── vendor-portal/       # Vendor Portal (Next.js)
│   │   └── ...
│   │
│   └── admin-portal/        # Admin Portal (Next.js)
│       └── ...
│
├── packages/
│   └── shared/              # Общий код
│       ├── src/
│       │   ├── types/       # TypeScript типы
│       │   ├── utils/       # Утилитарные функции
│       │   └── components/  # Общие компоненты
│       └── package.json
│
├── docs/                    # Документация
├── scripts/                 # Утилитарные скрипты
├── docker/                  # Docker конфигурации
├── docker-compose.dev.yml   # Docker Compose для разработки
├── package.json            # Корневой package.json с workspace скриптами
└── README.md               # README проекта
```

---

## Запуск приложения

### Запустить все сервисы

```bash
# Сначала запустить Docker сервисы
docker-compose -f docker-compose.dev.yml up -d

# Подождать, пока сервисы будут готовы (10-15 секунд)
sleep 10

# Запустить все серверы разработки
npm run dev
```

Это запустит:
- **Backend API:** http://localhost:3001
- **Client App:** http://localhost:3000
- **Vendor Portal:** http://localhost:3002
- **Admin Portal:** http://localhost:3003
- **API Docs (Swagger):** http://localhost:3001/api/docs

### Запустить отдельные сервисы

```bash
# Только Backend
npm run dev:server

# Только Client App
npm run dev:client

# Только Vendor Portal
npm run dev:vendor

# Только Admin Portal
npm run dev:admin
```

### Проверить настройку

1. **Проверить Backend API:**
   ```bash
   curl http://localhost:3001/health
   ```

2. **Проверить базу данных:**
   ```bash
   npm run db:studio
   # Должен открыть Prisma Studio на http://localhost:5555
   ```

3. **Проверить Keycloak:**
   - Открыть http://localhost:8080
   - Войти с учетными данными администратора

4. **Проверить MailHog:**
   - Открыть http://localhost:8025
   - Просмотреть тестовые emails

---

## Рабочий процесс разработки

### 1. Создать ветку функции

```bash
# Обновить ветку develop
git checkout develop
git pull origin develop

# Создать ветку функции
git checkout -b feature/your-feature-name
```

### 2. Внести изменения

- Написать код, следуя конвенциям проекта
- Написать тесты для новых функций
- Обновить документацию при необходимости
- Следовать конвенциям сообщений коммитов

### 3. Протестировать изменения

```bash
# Запустить unit тесты
npm run test

# Запустить линтинг
npm run lint

# Проверить ошибки TypeScript
npm run type-check
```

### 4. Закоммитить изменения

```bash
# Добавить изменения
git add .

# Закоммитить с conventional commit сообщением
git commit -m "feat(module): add new feature"

# Отправить в удаленный репозиторий
git push origin feature/your-feature-name
```

### 5. Создать Pull Request

- Создать PR из ветки функции в `develop`
- Заполнить шаблон PR
- Запросить ревью у членов команды
- Дождаться прохождения проверок CI
- Исправить комментарии ревью
- Слить после одобрения

### Конвенция сообщений коммитов

```
type(scope): description

Примеры:
feat(auth): add OAuth2 login
fix(orders): resolve payment status update
docs(readme): update installation steps
refactor(services): optimize database queries
test(orders): add unit tests for order service
```

**Типы:** `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`

---

## Общие задачи

### Добавление нового API эндпоинта

1. **Создать/Обновить контроллер:**
   ```typescript
   // apps/server/src/module/module.controller.ts
   @Post('endpoint')
   @UseGuards(JwtAuthGuard)
   async createEndpoint(@Body() dto: CreateDto) {
     return this.service.create(dto);
   }
   ```

2. **Создать/Обновить сервис:**
   ```typescript
   // apps/server/src/module/module.service.ts
   async create(dto: CreateDto) {
     // Бизнес-логика
   }
   ```

3. **Создать DTO:**
   ```typescript
   // apps/server/src/module/dto/create.dto.ts
   export class CreateDto {
     @IsString()
     name: string;
   }
   ```

### Добавление новой страницы Frontend

1. **Создать страницу:**
   ```typescript
   // apps/client/src/app/new-page/page.tsx
   export default function NewPage() {
     return <div>New Page</div>;
   }
   ```

2. **Добавить навигацию:**
   ```typescript
   // Обновить компонент навигации
   <Link href="/new-page">New Page</Link>
   ```

### Миграция базы данных

```bash
# Создать миграцию
npm run db:migrate:create -- --name migration_name

# Применить миграцию
npm run db:migrate

# Откатить (если необходимо)
npm run db:migrate:rollback
```

---

## Устранение неполадок

### Общие проблемы

#### 1. Порт уже используется

```bash
# Найти процесс, использующий порт
lsof -i :3001

# Убить процесс
kill -9 <PID>

# Или использовать другой порт
PORT=3002 npm run dev:server
```

#### 2. Ошибка подключения к базе данных

```bash
# Проверить Docker сервисы
docker-compose -f docker-compose.dev.yml ps

# Перезапустить PostgreSQL
docker-compose -f docker-compose.dev.yml restart postgres

# Проверить строку подключения в .env
echo $DATABASE_URL
```

#### 3. Keycloak не работает

```bash
# Проверить логи Keycloak
docker-compose -f docker-compose.dev.yml logs keycloak

# Перезапустить Keycloak
docker-compose -f docker-compose.dev.yml restart keycloak

# Перезапустить скрипт настройки
./scripts/setup-keycloak.sh
```

#### 4. Ошибки "Module Not Found"

```bash
# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install

# Очистить кэш Next.js
rm -rf apps/*/.next
```

---

## Следующие шаги

### После настройки

1. ✅ **Проверить установку:** Все сервисы запущены
2. ✅ **Запустить тесты:** `npm run test`
3. ✅ **Изучить кодовую базу:** Прочитать ключевые файлы
4. ✅ **Настроить IDE:** Установить рекомендуемые расширения
5. ✅ **Прочитать документацию:** Просмотреть папку `docs/`

### Ресурсы для обучения

- **NestJS:** [docs.nestjs.com](https://docs.nestjs.com/)
- **Next.js:** [nextjs.org/docs](https://nextjs.org/docs)
- **Prisma:** [prisma.io/docs](https://www.prisma.io/docs)
- **TypeScript:** [typescriptlang.org/docs](https://www.typescriptlang.org/docs/)

### Первые задачи

1. **Исправить ошибку:** Выбрать issue из GitHub
2. **Добавить функцию:** Начать с небольшой функции
3. **Улучшить документацию:** Обновить документацию
4. **Написать тесты:** Добавить тесты для существующего кода
5. **Code Review:** Просмотреть открытые PR

---

## Приложение

### Связанные документы

- [Полное руководство по настройке](docs/COMPLETE_SETUP_GUIDE.md)
- [Руководство по разработке](docs/DEVELOPMENT.md)
- [Настройка Docker](docs/DOCKER_SETUP.md)
- [Настройка Keycloak](docs/KEYCLOAK_SETUP.md)
- [Справочник API](docs/API_REFERENCE.md)

---

**Последнее обновление:** 7 декабря 2025
**Поддерживается:** Командой разработки


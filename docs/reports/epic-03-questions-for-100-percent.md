# Вопросы для приведения Epic 3 к 100% готовности

**Дата:** 2025-12-03
**Epic:** E-003 Services Catalog
**Цель:** Уточнить все неясные моменты для полной готовности к реализации

---

## 🔴 Критические вопросы (блокируют 100% готовность)

### 1. Проверка активных заказов при удалении услуги (US-018)

**Текущая ситуация:**

- US-018 требует: "Нельзя удалить если есть активные заказы"
- Текущая реализация: soft delete без проверки заказов

**Вопросы:**

1. **Какие статусы заказов считаются "активными"?**
   - Только `PENDING`, `CONFIRMED`, `IN_PROGRESS`?
   - Или также `COMPLETED` и `CANCELLED` не считаются активными?
   - Нужно ли учитывать `REFUNDED`?

2. **Что делать, если у услуги есть активные заказы?**
   - Возвращать ошибку 400 с сообщением "Cannot delete service with active orders"?
   - Или разрешить удаление, но предупредить пользователя?
   - Нужно ли показывать количество активных заказов в сообщении об ошибке?

3. **Проверка на уровне OrderItem или Order?**
   - Проверять наличие `OrderItem` с `serviceId` и связанного `Order` со статусом != COMPLETED/CANCELLED?
   - Или достаточно проверить наличие любых `OrderItem` для этой услуги?

4. **Должен ли Admin иметь возможность удалить услугу с активными заказами?**
   - Или правило одинаково для всех (включая Admin)?

---

### 2. Endpoint path для "мои услуги" (US-013)

**Текущая ситуация:**

- Требование в US-013: `GET /services/my`
- Текущая реализация: `GET /services/vendor/my`

**Вопросы:**

1. **Какой endpoint path правильный?**
   - `GET /services/my` (как в User Story)?
   - `GET /services/vendor/my` (как реализовано)?
   - Или оба варианта допустимы?

2. **Нужна ли версия для других ролей?**
   - `GET /services/client/my` - для клиентов (их заказы)?
   - Или это не относится к Epic 3?

3. **Должен ли endpoint быть доступен только для VENDOR?**
   - Или другие роли тоже могут видеть "свои услуги" (если это применимо)?

---

### 3. Валидация минимальной длины search query (US-014)

**Текущая ситуация:**

- US-014 требует: "Минимум 2 символа для поиска"
- Текущая реализация: нет валидации минимальной длины

**Вопросы:**

1. **Нужна ли валидация на уровне DTO/Controller?**
   - Или достаточно проверки в Service?
   - Какой HTTP статус возвращать: 400 Bad Request?

2. **Что делать с пустым search query?**
   - Игнорировать (не применять фильтр)?
   - Или возвращать ошибку валидации?

3. **Нужна ли валидация для других параметров?**
   - Минимальная/максимальная длина для других строковых параметров?
   - Валидация диапазона для page/limit?

---

## 🟡 Важные вопросы (улучшают качество)

### 4. Дополнительные фильтры (minPrice/maxPrice)

**Текущая ситуация:**

- Реализация включает фильтры `minPrice` и `maxPrice`
- В User Stories эти фильтры не упомянуты

**Вопросы:**

1. **Нужны ли эти фильтры в MVP?**
   - Оставить как есть (улучшение)?
   - Или убрать до Phase 2?

2. **Если оставляем, нужно ли добавить в User Stories?**
   - Обновить US-013 или создать отдельную US?

---

### 5. Проверка статуса услуги в публичном каталоге (US-013, US-016)

**Текущая ситуация:**

- US-013: "Возвращает только ACTIVE услуги"
- US-016: "404 если услуга INACTIVE или vendor не APPROVED"
- Текущая реализация: фильтрует по `status: ACTIVE` в публичном каталоге

**Вопросы:**

1. **Должны ли показываться услуги со статусом `PENDING_REVIEW`?**
   - Только `ACTIVE`?
   - Или `PENDING_REVIEW` тоже показывать (для модерации)?

2. **Что делать с `INACTIVE` услугами?**
   - Полностью скрывать из публичного каталога?
   - Или показывать, но с пометкой?

3. **Должен ли `GET /services/:id` проверять статус?**
   - US-016 говорит "404 если INACTIVE"
   - Но может быть нужен доступ для владельца (vendor)?

---

### 6. Валидация categoryId при создании/обновлении

**Текущая ситуация:**

- `categoryId` опционален в CreateServiceDto
- Валидация существования категории реализована

**Вопросы:**

1. **Должен ли `categoryId` быть обязательным?**
   - Или опциональным (как сейчас)?
   - Нужна ли валидация, что категория активна (`isActive: true`)?

2. **Что делать при обновлении categoryId на несуществующий?**
   - Текущая реализация: BadRequestException
   - Это правильное поведение?

---

### 7. Формат ответа для деталей услуги (US-016)

**Текущая ситуация:**

- US-016 требует включить данные vendor: `businessName`, `contactPhone`, `address`, `description`
- Текущая реализация: возвращает `businessName`, `rating`, `reviewCount`

**Вопросы:**

1. **Какие поля vendor должны быть в ответе?**
   - Только `businessName`, `rating`, `reviewCount` (как сейчас)?
   - Или также `contactPhone`, `address`, `description` (как в US-016)?
   - Нужно ли включать `contactEmail`?

2. **Нужна ли разная информация для разных ролей?**
   - Для публичного доступа: меньше данных?
   - Для владельца: полная информация?

---

### 8. Обработка изображений (images)

**Текущая ситуация:**

- `images` - массив строк (URLs)
- Валидация: `IsString({ each: true })`

**Вопросы:**

1. **Нужна ли валидация URL формата?**
   - Текущая реализация: только проверка, что это строка
   - Нужна ли валидация `@IsUrl()` для каждого элемента?

2. **Есть ли ограничение на количество изображений?**
   - Максимум 5, 10, или без ограничений?

3. **Нужна ли валидация размера/формата изображений?**
   - Или это задача фронтенда/отдельного сервиса загрузки?

---

### 9. Сортировка в публичном каталоге (US-013)

**Текущая ситуация:**

- US-013: "Сортировка по умолчанию: createdAt desc"
- Текущая реализация: `orderBy: { createdAt: 'desc' }`

**Вопросы:**

1. **Нужна ли поддержка других вариантов сортировки?**
   - По цене (asc/desc)?
   - По названию (alphabetical)?
   - По рейтингу vendor?

2. **Если да, нужно ли добавить query параметр `sortBy`?**
   - Или это для Phase 2?

---

### 10. Пагинация - максимальный limit

**Текущая ситуация:**

- US-013: "limit (max 50)"
- Текущая реализация: нет проверки максимального limit

**Вопросы:**

1. **Нужна ли валидация максимального limit?**
   - Установить максимум 50 (как в US-013)?
   - Или другое значение?

2. **Что делать, если передан limit > 50?**
   - Автоматически ограничить до 50?
   - Или вернуть ошибку валидации?

---

## 🟢 Дополнительные вопросы (для полноты картины)

### 11. Логирование и мониторинг

**Вопросы:**

1. **Нужно ли логировать создание/обновление/удаление услуг?**
   - Текущая реализация: есть базовое логирование
   - Нужны ли дополнительные метрики?

2. **Нужно ли отслеживать популярность услуг?**
   - Количество просмотров?
   - Или это для Phase 2?

---

### 12. Кэширование

**Вопросы:**

1. **Нужно ли кэширование для публичного каталога?**
   - Redis кэш для списка услуг?
   - Или это для Phase 2?

---

### 13. Интеграция с Epic 4 (Categories)

**Вопросы:**

1. **Можно ли создавать услуги без категории?**
   - Текущая реализация: `categoryId` опционален
   - Это правильное поведение для MVP?

2. **Что делать, если категория удалена?**
   - Текущая реализация: `onDelete: SetNull` - categoryId становится null
   - Это правильное поведение?

---

## 📋 Сводная таблица вопросов

| #   | Вопрос                                     | Приоритет         | Блокирует 100%? |
| --- | ------------------------------------------ | ----------------- | --------------- |
| 1   | Проверка активных заказов при удалении     | 🔴 Критический    | ✅ Да           |
| 2   | Endpoint path для "мои услуги"             | 🔴 Критический    | ✅ Да           |
| 3   | Валидация минимальной длины search         | 🔴 Критический    | ✅ Да           |
| 4   | Дополнительные фильтры (minPrice/maxPrice) | 🟡 Важный         | ❌ Нет          |
| 5   | Проверка статуса услуги в каталоге         | 🟡 Важный         | ❌ Нет          |
| 6   | Валидация categoryId                       | 🟡 Важный         | ❌ Нет          |
| 7   | Формат ответа для деталей услуги           | 🟡 Важный         | ❌ Нет          |
| 8   | Обработка изображений                      | 🟡 Важный         | ❌ Нет          |
| 9   | Сортировка в каталоге                      | 🟢 Дополнительный | ❌ Нет          |
| 10  | Пагинация - максимальный limit             | 🟡 Важный         | ❌ Нет          |
| 11  | Логирование и мониторинг                   | 🟢 Дополнительный | ❌ Нет          |
| 12  | Кэширование                                | 🟢 Дополнительный | ❌ Нет          |
| 13  | Интеграция с Epic 4                        | 🟡 Важный         | ❌ Нет          |

---

## ✅ Рекомендации по приоритетам

### Для достижения 100% готовности (критично):

1. ✅ Ответить на вопросы #1, #2, #3
2. ✅ Обновить User Stories с уточнениями
3. ✅ Обновить реализацию согласно ответам

### Для улучшения качества (важно, но не блокирует):

4. ✅ Ответить на вопросы #4, #5, #6, #7, #8, #10, #13
5. ✅ Принять решение по дополнительным функциям

### Для будущих улучшений (можно отложить):

6. ✅ Вопросы #9, #11, #12 - для Phase 2

---

## 📝 Следующие шаги

1. Ответить на все критические вопросы (#1-3)
2. Обновить User Stories с уточнениями
3. Обновить реализацию согласно ответам
4. Провести финальную проверку готовности

# Epic 3: Services Catalog - Тестирование

**Дата:** 2025-12-03
**Тестировщик:** AI Assistant
**Статус:** В процессе

---

## 📋 План тестирования

### 1. Миграция Prisma

- [ ] Применить миграцию `make_category_id_required_and_restrict_delete`
- [ ] Проверить, что все услуги имеют categoryId
- [ ] Проверить, что onDelete: Restrict работает

### 2. Создание услуги (US-012)

- [ ] `POST /services` - создание с валидными данными
- [ ] `POST /services` - categoryId обязателен (ошибка без него)
- [ ] `POST /services` - валидация изображений (URL формат, максимум 5)
- [ ] `POST /services` - только APPROVED vendors могут создавать
- [ ] `POST /services` - категория должна быть активна

### 3. Публичный каталог (US-013)

- [ ] `GET /services` - публичный доступ работает
- [ ] `GET /services` - только ACTIVE услуги
- [ ] `GET /services` - только APPROVED vendors
- [ ] `GET /services` - пагинация работает (limit max 10)
- [ ] `GET /services?minPrice=100&maxPrice=500` - фильтры по цене
- [ ] `GET /services?sortBy=price_asc` - сортировка работает
- [ ] `GET /services?sortBy=name_desc` - сортировка по названию
- [ ] `GET /services?sortBy=rating_desc` - сортировка по рейтингу

### 4. Поиск услуг (US-014)

- [ ] `GET /services?search=test` - поиск работает (>= 2 символа)
- [ ] `GET /services?search=t` - ошибка 400 (< 2 символа)
- [ ] `GET /services?search=` - пустой search игнорируется
- [ ] `GET /services?search=похороны` - case-insensitive поиск

### 5. Детали услуги (US-016)

- [ ] `GET /services/:id` - публичный доступ
- [ ] `GET /services/:id` - возвращает vendor поля (contactPhone, address, etc.)
- [ ] `GET /services/:id` - 404 для INACTIVE услуги (публичный доступ)
- [ ] `GET /services/:id` - владелец может видеть свои услуги с любым статусом

### 6. Обновление услуги (US-017)

- [ ] `PATCH /services/:id` - обновление работает
- [ ] `PATCH /services/:id` - только владелец может обновить
- [ ] `PATCH /services/:id` - валидация categoryId (должна быть активна)

### 7. Удаление услуги (US-018)

- [ ] `DELETE /services/:id` - soft delete работает
- [ ] `DELETE /services/:id` - нельзя удалить с активными заказами (Vendor)
- [ ] `DELETE /services/:id` - Admin может удалить с каскадным удалением
- [ ] `DELETE /services/:id` - только владелец может удалить

### 8. Мои услуги (Vendor)

- [ ] `GET /services/vendor/my` - возвращает услуги vendor
- [ ] `GET /services/vendor/my` - показывает все статусы для владельца

---

## 🧪 Результаты тестирования

### 1. Миграция Prisma

**Статус:** ⏳ В процессе

#### Результаты:

- ⏳ Миграция применена
- ⏳ Проверка данных

### 2. Создание услуги (US-012)

**Статус:** ⏳ В процессе

#### Результаты:

- ⏳ Тестирование...

### 3. Публичный каталог (US-013)

**Статус:** ⏳ В процессе

#### Результаты:

- ⏳ Тестирование...

### 4. Поиск услуг (US-014)

**Статус:** ⏳ В процессе

#### Результаты:

- ⏳ Тестирование...

### 5. Детали услуги (US-016)

**Статус:** ⏳ В процессе

#### Результаты:

- ⏳ Тестирование...

### 6. Обновление услуги (US-017)

**Статус:** ⏳ В процессе

#### Результаты:

- ⏳ Тестирование...

### 7. Удаление услуги (US-018)

**Статус:** ⏳ В процессе

#### Результаты:

- ⏳ Тестирование...

### 8. Мои услуги (Vendor)

**Статус:** ⏳ В процессе

#### Результаты:

- ⏳ Тестирование...

---

## 📝 Заметки

- Тестирование выполняется через Swagger UI и curl
- Используется локальная среда разработки
- Keycloak должен быть настроен и доступен

# Epic 8: Email Notifications - План тестирования

**Дата:** 2025-12-03
**SMTP:** MailHog (localhost:1025) для локального тестирования

---

## 🧪 Тестовые сценарии

### Тест 1: Welcome Email (Регистрация пользователя)

**Endpoint:** `POST /api/auth/register`

**Шаги:**

1. Открыть Swagger UI: http://localhost:3001/api/docs
2. Выполнить `POST /api/auth/register`:
   ```json
   {
     "email": "test@example.com",
     "password": "password123",
     "firstName": "Test",
     "lastName": "User"
   }
   ```
3. Проверить логи сервера:
   ```bash
   docker-compose logs -f server | grep -i email
   ```
4. Открыть MailHog UI: http://localhost:8025
5. Проверить наличие письма "Welcome to Memento Mori"

**Ожидаемый результат:**

- ✅ Email отправлен успешно
- ✅ Лог: "Email sent to test@example.com: Welcome to Memento Mori"
- ✅ Письмо видно в MailHog UI

---

### Тест 2: Order Confirmation (Создание заказа)

**Endpoint:** `POST /api/orders`

**Предварительные условия:**

- Пользователь зарегистрирован и авторизован (client)
- Существует активный сервис
- Существует категория

**Шаги:**

1. Получить токен через `POST /api/auth/login`
2. Авторизоваться в Swagger (кнопка "Authorize")
3. Создать заказ через `POST /api/orders`:
   ```json
   {
     "items": [
       {
         "serviceId": "service-uuid",
         "quantity": 1,
         "notes": "Test order"
       }
     ],
     "notes": "Test order"
   }
   ```
4. Проверить логи сервера
5. Проверить MailHog UI

**Ожидаемый результат:**

- ✅ Email "Order Confirmation" отправлен клиенту
- ✅ Письмо содержит номер заказа, список товаров, итоговую сумму

---

### Тест 3: Order Status Update (Изменение статуса заказа)

**Endpoint:** `PATCH /api/orders/:id/status`

**Предварительные условия:**

- Существует заказ со статусом PENDING

**Шаги:**

1. Авторизоваться как vendor или admin
2. Изменить статус заказа через `PATCH /api/orders/:id/status`:
   ```json
   {
     "status": "CONFIRMED",
     "reason": "Order confirmed by vendor"
   }
   ```
3. Проверить логи и MailHog

**Ожидаемый результат:**

- ✅ Email "Order Status Update" отправлен клиенту
- ✅ Письмо содержит новый статус и сообщение

---

### Тест 4: Payment Confirmation (Успешная оплата)

**Endpoint:** `POST /payments/confirm`

**Предварительные условия:**

- Существует заказ с Payment Intent

**Шаги:**

1. Авторизоваться как client
2. Подтвердить платеж через `POST /payments/confirm`:
   ```json
   {
     "paymentIntentId": "pi_xxx"
   }
   ```
3. Проверить логи и MailHog

**Ожидаемый результат:**

- ✅ Email "Order Confirmation" отправлен клиенту
- ✅ Письмо содержит подтверждение оплаты

---

### Тест 5: Payment Failed (Неудачная оплата)

**Endpoint:** Webhook или `POST /payments/confirm` (с невалидным paymentIntentId)

**Ожидаемый результат:**

- ✅ Email "Order Status Update" с статусом PAYMENT_FAILED отправлен клиенту

---

### Тест 6: Vendor Approval (Одобрение поставщика)

**Endpoint:** `PATCH /api/vendors/:id/status` (admin only)

**Предварительные условия:**

- Существует vendor со статусом PENDING

**Шаги:**

1. Авторизоваться как admin
2. Изменить статус vendor через `PATCH /api/vendors/:id/status`:
   ```json
   {
     "status": "APPROVED"
   }
   ```
3. Проверить логи и MailHog

**Ожидаемый результат:**

- ✅ Email "Your Vendor Account has been Approved" отправлен vendor
- ✅ Письмо содержит ссылку на vendor dashboard

---

### Тест 7: Vendor Rejection (Отклонение поставщика)

**Endpoint:** `PATCH /api/vendors/:id/status` (admin only)

**Шаги:**

1. Авторизоваться как admin
2. Изменить статус vendor на REJECTED
3. Проверить логи и MailHog

**Ожидаемый результат:**

- ✅ Email "Update on Your Vendor Application" отправлен vendor
- ✅ Письмо содержит причину отклонения

---

### Тест 8: Service Status Update (Модерация услуги)

**Endpoint:** `PATCH /services/:id/status` (admin only)

**Предварительные условия:**

- Существует услуга со статусом PENDING_REVIEW

**Шаги:**

1. Авторизоваться как admin
2. Изменить статус услуги через `PATCH /services/:id/status`:
   ```json
   {
     "status": "ACTIVE"
   }
   ```
3. Проверить логи и MailHog

**Ожидаемый результат:**

- ✅ Email "Service Status Update" отправлен vendor
- ✅ Письмо содержит новый статус услуги

---

## 📊 Чек-лист тестирования

- [ ] Тест 1: Welcome Email
- [ ] Тест 2: Order Confirmation
- [ ] Тест 3: Order Status Update
- [ ] Тест 4: Payment Confirmation
- [ ] Тест 5: Payment Failed
- [ ] Тест 6: Vendor Approval
- [ ] Тест 7: Vendor Rejection
- [ ] Тест 8: Service Status Update

---

## 🔍 Проверка логов

```bash
# Просмотр всех логов email
docker-compose logs -f server | grep -i email

# Просмотр логов отправки
docker-compose logs -f server | grep -i "Email sent"

# Просмотр ошибок
docker-compose logs -f server | grep -i "Failed to send email"
```

---

## 📧 MailHog UI

**URL:** http://localhost:8025

**Функции:**

- Просмотр всех отправленных email
- Просмотр содержимого email (HTML)
- Просмотр заголовков
- Поиск по получателю/отправителю

---

**Последнее обновление:** 2025-12-03

# US-001: Регистрация Client пользователя

**Epic:** E-001 Authentication & Authorization  
**Portal:** Backend  
**Приоритет:** 🔴 Must Have  
**Story Points:** 3  
**Статус:** ✅ Выполнено

---

## User Story

**Как Client**, я хочу зарегистрироваться на платформе через Keycloak, чтобы получить доступ к услугам

---

## Acceptance Criteria

- [ ] Endpoint `POST /auth/register` принимает данные регистрации
- [ ] Форма регистрации с полями: email, username, password, firstName, lastName
- [ ] Валидация email формата (правильный email)
- [ ] Валидация password (управляется Keycloak)
- [ ] Проверка уникальности email в локальной БД
- [ ] Создание пользователя в Keycloak через Admin API
- [ ] Назначение роли `client` в Keycloak
- [ ] Создание записи User в локальной БД (пароль НЕ хранится)
- [ ] Автоматический логин после регистрации (возврат токенов)
- [ ] Возврат данных пользователя (без пароля)

---

## API Specification

### Request

```http
POST /auth/register
Content-Type: application/json

{
  "email": "client@example.com",
  "password": "SecurePass123",
  "firstName": "Иван",
  "lastName": "Петров"
}
```

### Response (Success - 201)

```json
{
  "id": "uuid",
  "email": "client@example.com",
  "firstName": "Иван",
  "lastName": "Петров",
  "role": "CLIENT",
  "createdAt": "2025-12-01T10:00:00Z"
}
```

**Примечание:** После регистрации пользователь автоматически логинится, токены возвращаются через `/auth/login`.

### Response (Error - 400)

```json
{
  "statusCode": 400,
  "message": ["email must be a valid email", "password must be at least 8 characters"],
  "error": "Bad Request"
}
```

### Response (Error - 409)

```json
{
  "statusCode": 409,
  "message": "User with this email already exists",
  "error": "Conflict"
}
```

---

## Technical Notes

- Использовать Keycloak Admin API для создания пользователя
- Пароль НЕ хранится в локальной БД (управляется Keycloak)
- Использовать `class-validator` для валидации DTO
- После регистрации пользователь автоматически логинится
- Пользователь синхронизируется с локальной БД после создания в Keycloak

---

## Database

```prisma
model User {
  id        String   @id @default(uuid()) // Keycloak user ID
  email     String   @unique
  password  String   // Пустая строка - пароль управляется Keycloak
  firstName String?
  lastName  String?
  role      Role     @default(CLIENT)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

enum Role {
  CLIENT
  VENDOR
  ADMIN
}
```

**Примечание:** Пароль не хранится в локальной БД, так как управляется Keycloak.

---

## Dependencies

- Нет (стартовая User Story)

---

## Test Cases

1. ✅ Успешная регистрация с валидными данными
2. ✅ Ошибка при невалидном email
3. ✅ Ошибка при коротком пароле
4. ✅ Ошибка при существующем email
5. ✅ Пароль не возвращается в response
6. ✅ Пользователь создан в Keycloak
7. ✅ Пользователь синхронизирован с локальной БД
8. ✅ Роль `client` назначена в Keycloak

---

## Definition of Done

- [ ] Код написан и работает
- [ ] Unit тесты написаны (покрытие > 80%)
- [ ] API документация обновлена (Swagger)
- [ ] Code review пройден
- [ ] Интеграционные тесты пройдены
